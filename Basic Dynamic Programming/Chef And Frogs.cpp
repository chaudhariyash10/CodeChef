#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, p, k;
    cin >> n >> k >> p;
    int arr[n];

    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
        
    }

    sort(sorted, sorted + n);

    bool dp[n + 1][n + 1];
}