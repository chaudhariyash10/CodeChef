#include <bits/stdc++.h>

using namespace std;

int minPrice(int n, int e, int h, int a, int b, int c, int price)
{
  
}

int main()
{
    long long t;
    cin >> t;
    while (t--)
    {
        int n, e, h, a, b, c;
        cin >> n >> e >> h >> a >> b >> c;
        cout << minPrice(n, e, h, a, b, c, 0) << endl;
    }
}